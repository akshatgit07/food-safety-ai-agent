# Pinned to major versions so a Render rebuild cannot silently pick up a breaking
# release. Only packages the backend actually imports are listed; langgraph,
# langchain, chromadb and loguru were declared but never imported, and chromadb
# alone added a multi-minute build for code that does not exist yet.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.8,<3.0
openai>=1.40,<4.0
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0
# Required by fastapi.testclient (starlette) in the backend test suite.
httpx>=0.27,<1.0
